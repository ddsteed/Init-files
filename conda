#!/usr/bin/env zsh
#-----------------------------------------------------------------------
# This script is to iniatize python conda/virtual environment
#---
# Author: Hao Feng (F1)
#
# Init Date:   Apr. 21, 2022
# last Date:   Apr. 07, 2023
#
# Copyright (c) 2022, RDS
#
#---
#   ai:       AI envs
#   dcmenv:   dcm conda environment
#   fpa:      Fatavay Physics based AI
#   mbforge:  MambaForge
#-----------------------------------------------------------------------

create_conda () {                                        
    name=$1                                              
                                                         
    if [[ ! -f $name ]]; then                            
#       init_conda $condabase
 
        py_version=`python3 --version | awk '{print $2}'`
        version=`echo ${py_version%.*}`
        conda create -y -p $name python=$version
    fi                                                   
                                                         
    conda activate $name                                 
    conda config --env --add channels conda-forge        

    # - two important tools
    conda install -y mamba -c conda-forge                
#   pip install pip-review -y
}                                                        
                                                         
#----------------
init_conda () {
    env=$1

    if [[ -f $env/etc/profile.d/conda.sh ]]; then
        source $env/etc/profile.d/conda.sh
        conda activate $env
        condabin=`dirname $CONDA_EXE`
    elif [[ -f $env/bin/activate ]]; then
        source $env/bin/activate
    else
        conda activate $env
        if [ $? -ne 0 ]; then
            echo
            echo "$env does NOT exist, create it firstly."
            echo "----------------------------------------------------"
            echo
            create_conda $env
        fi
    fi
}

#----------------
pipup () {
    pip-review -a
}

#----------------
condauc () {
    conda $1 --all -y
}

#----------------
condaup () {
    condauc update
}

#----------------
condaclean () {
    condauc clean
}

#----------------
acondaup () {
    conda update anaconda --all -y
}

#----------------
conda_export () {
    conda env export --no-builds | grep -v "^prefix: " > $1
}

#----------------
conda_from_file () {
    mamba env update --file $1
}

#----------------
cdea () {
    conda deactivate
}

#----------------
cact () {
    conda activate $1
}

#----------------
cinfo () {
    conda info
}

#----------------
cel () {
    conda env list
}

#----------------
jnb () {
    $condabin/jupyter-notebook
}

#----------------
jlab () {
    $condabin/jupyter-lab
}

#----#----------------
install_conda_basic () {                                 
    basic=(jupyter matplotlib numpy pandas scipy seaborn)
    mamba install -y -c conda-forge ${basic[@]}          
}                                                        

#----------------
pyenv () {
    python3 -m venv $1
}

#---------------------------------------------------------
# main function

runname=`basename $0`
os=`uname`

mb_home=/opt/Homebrew/Caskroom/mambaforge/base
ai_home=~/Research/AI/AI
fpa_home=~/Research/AI/FPA
dcm_home=~/Research/FORECAST/DCM

case $runname in
    ai | aiconda)
        export condaenv=${ai_home}/envs
        ;;
    fpa)
        export condaenv=${fpa_home}/envs
        ;;
    dcmenv)
        export condaenv=${dcm_home}/envs
        ;;
    mbforge)
        export condaenv=${mb_home}
        ;;
    *)
        export condaenv=${mb_home}
        ;;
esac

export PATH=${condaenv}/bin:$PATH
init_conda $condaenv
